{
  "name": "tempus-sales-copilot",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm run ingest && vite",
    "build": "npm run ingest && tsc -b && vite build",
    "preview": "vite preview",
    "eval": "tsx scripts/eval.ts",
    "smoke": "tsx scripts/smoke.ts",
    "adversarial": "tsx scripts/adversarial.ts",
    "gate4": "tsx scripts/gate4.ts",
    "verify": "npm run ingest && tsc -b && npm run style && npm run eval && npm run gate4 && npm run adversarial",
    "ingest": "tsx scripts/ingest.ts",
    "style": "tsx scripts/no-em-dash.ts"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "lucide-react": "latest",
    "mapbox-gl": "^3.29.0",
    "react": "latest",
    "react-dom": "latest",
    "vite": "latest"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "tsx": "^4.23.12",
    "typescript": "latest"
  }
}
